# ui/

shadcn/ui primitives live here once initialized (Phase 3+).
These are copied-in components (not an npm dependency), so this
folder is intentionally empty at scaffold time — nothing to
install yet, nothing to audit for now.
