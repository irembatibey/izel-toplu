/**
 * MDX content utilities.
 *
 * Phase 1 scope: this file exists so the intended architecture
 * (content separated from presentation, parsed via gray-matter)
 * is visible in the project structure. Actual read/parse logic
 * is implemented in the content-layer phase, once real .mdx
 * service files exist to build against.
 */

// Placeholder export so this module is valid TypeScript until
// implemented. Removed once real functions are added.
export {};
