# Dependencies
node_modules/

# Next.js build output
.next/
out/

# Environment variables — never commit real values.
# .env.example is intentionally NOT ignored (see that file).
.env
.env.local
.env.*.local

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Editor/OS
.DS_Store
.vscode/
.idea/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vercel (not used for deployment, but ignored defensively
# in case anyone runs `vercel` locally during development)
.vercel
